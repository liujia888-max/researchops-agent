"""researchops.mcp — MCP servers built on the core package.

Currently hosts ``labops`` (remote GPU-lab orchestration). Each server here is a thin
transport adapter; all logic lives in the sibling ``researchops.labops`` layer.
"""
